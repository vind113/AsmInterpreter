namespace App.X86
{
    public enum Registers
    {
        rax, eax, ax, ah, al,
        rbx, ebx, bx, bh, bl,
        rcx, ecx, cx, ch, cl,
        rdx, edx, dx, dh, dl,

        rbp, ebp, bp, bpl,

        rsi, esi, si, sil,
        rdi, edi, di, dil,

        rsp, esp, sp, spl
    }
}
